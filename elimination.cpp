#include <bits/stdc++.h>

using namespace std;

int solve(int a, int b, int c, int d) {

}

int main() {
    ios::sync_with_stdio(false);
    int T, a, b, c, d;
    cin >> T;
    while (T--) {
        cin >> a >> b >> c >> d;
        cout << solve(a, b, c, d) << endl;
    }
    return 0;
}